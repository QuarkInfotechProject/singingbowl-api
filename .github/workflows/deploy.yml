name: 🚀 Deploy Laravel API (Smart + Self-Healing)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy via SSH
    runs-on: ubuntu-latest

    env:
      DEBUG: true # Set to true for detailed SSH logging

    steps:
      # ======================================================
      # 1️⃣ CHECKOUT REPOSITORY
      # ======================================================
      - name: ⏳ Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Full history for proper git operations

      # ======================================================
      # 2️⃣ SETUP SSH
      # ======================================================
      - name: 🔐 Setup SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SINGINGBOWL_SSH_PRIVATE_KEY }}

      # ======================================================
      # 3️⃣ DEPLOY TO VPS
      # ======================================================
      - name: 🚀 Deploy Laravel API
        env:
          VPS_USER: ${{ secrets.SINGINGBOWL_USER }}
          VPS_HOST: ${{ secrets.QUARK_VPS_HOST }}
          VPS_PORT: ${{ secrets.QUARK_VPS_PORT }}
          GIT_AUTH_TOKEN: ${{ secrets.GIT_AUTH_TOKEN }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          DEBUG: ${{ env.DEBUG }}
          REPO_SLUG: ${{ github.repository }}
        run: |
          echo "🌍 Connecting to VPS: $VPS_HOST:$VPS_PORT as $VPS_USER"
          echo "📂 Target Path: $DEPLOY_PATH"

          if [ "$DEBUG" = "true" ]; then
            echo "🧩 DEBUG MODE ENABLED — will print commands as they execute"
            set -x
          fi

          ssh -o StrictHostKeyChecking=no $VPS_USER@$VPS_HOST -p $VPS_PORT "
            set -e

            echo '🖥️ Connected to:' \$(hostname)
            echo '📦 Starting Laravel deployment in:' $DEPLOY_PATH

            # ================================
            # CHECK OR CREATE DEPLOYMENT DIRECTORY
            # ================================
            if [ ! -d '$DEPLOY_PATH' ]; then
              echo '📁 Directory not found — creating: $DEPLOY_PATH'
              mkdir -p '$DEPLOY_PATH'
              echo '🆕 New deployment directory created.'
            fi
            cd '$DEPLOY_PATH'

            # ================================
            # GIT OPERATIONS
            # ================================
            echo '🔄 Updating repository...'
            if [ ! -d '.git' ]; then
              echo '⚠️ .git folder missing — cloning repository fresh...'

              if [ -z '${GIT_AUTH_TOKEN}' ]; then
                echo '❌ ERROR: GIT_AUTH_TOKEN not set or invalid. Check your GitHub Secrets.'
                exit 1
              fi

              git clone https://x-access-token:${GIT_AUTH_TOKEN}@github.com/${REPO_SLUG}.git .
            else
              git fetch origin main
              git reset --hard origin/main
            fi

            # ================================
            # MOVE TO SYSTEM DIRECTORY (where artisan lives)
            # ================================
            if [ ! -d 'system' ]; then
              echo '❌ ERROR: Laravel system directory not found at $DEPLOY_PATH/system'
              exit 1
            fi

            cd system
            echo '📂 Entered Laravel system directory: ' \$(pwd)

            # ================================
            # COMPOSER INSTALL
            # ================================
            echo '📦 Installing Composer dependencies...'
            /usr/local/lsws/lsphp81/bin/php /usr/bin/composer install --no-interaction --prefer-dist --optimize-autoloader

            # ================================
            # ARTISAN COMMANDS
            # ================================
            echo '⚙️ Running Laravel maintenance tasks...'
            /usr/local/lsws/lsphp81/bin/php artisan key:generate || true
            /usr/local/lsws/lsphp81/bin/php artisan migrate --force
            /usr/local/lsws/lsphp81/bin/php artisan optimize:clear
            /usr/local/lsws/lsphp81/bin/php artisan config:cache

            echo '✅ Laravel deployment complete!'
            echo '📅 Finished at:' \$(date)
          "

          if [ "$DEBUG" = "true" ]; then
            set +x
            echo "🧩 DEBUG MODE DISABLED — end of logs"
          fi
